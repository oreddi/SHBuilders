import LoginForm from './LoginForm';

export const metadata = {
  title: 'Login - SH Builders',
  description: 'Team Access Portal',
};

export default function LoginPage() {
  return <LoginForm />;
}
